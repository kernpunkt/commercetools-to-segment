# Segment Integration Configuration
# Copy this file to .env and fill in your actual values

# Required: Segment Write Key for API Authentication
# Get your write key from Segment dashboard: Settings → API Keys
# This key is used to authenticate requests to Segment's Identify API
# Without this key, the application cannot send customer data to Segment
SEGMENT_WRITE_KEY=your-segment-write-key-here

# Optional: Vercel Deployment URLs
# These are useful for documentation and testing purposes

# Base URL of the Vercel deployment
# Format: https://your-project-name.vercel.app
VERCEL_URL=https://your-project-name.vercel.app

# Full webhook endpoint URL for Commercetools configuration
# This is the exact URL to use when setting up webhook subscriptions in Commercetools
WEBHOOK_ENDPOINT_URL=https://your-project-name.vercel.app/api/webhook

# Optional: Vercel Preview Protection Bypass Secret
# Only needed if you want to allow external services (like Commercetools) to access protected preview deployments
# Get this from Vercel project settings → Deployment Protection
# Keep this secret and never commit it to version control
VERCEL_PROTECTION_BYPASS_SECRET=your-vercel-protection-bypass-secret-here
