{
  "builds": [
    { "src": "api/**/*.ts", "use": "@vercel/node", "config": { "includeFiles": ["src/**/*"] } }
  ],
  "routes": [
    { "src": "/api/(.*)", "dest": "api/$1.ts" }
  ],
  "installCommand": "corepack enable && corepack prepare pnpm@10.17.0 --activate && pnpm install --no-frozen-lockfile"
}